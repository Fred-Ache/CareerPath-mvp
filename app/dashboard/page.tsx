import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function DashboardPage() {
  return (
    <PlaceholderPage
      title="Student dashboard"
      description="This area will show your profile, latest assessment, saved careers, and mentor requests after authentication is in place."
      nextHref="/"
      nextLabel="Back home"
    />
  );
}
