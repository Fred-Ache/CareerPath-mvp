import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function RoadmapPage() {
  return (
    <PlaceholderPage
      title="Personal roadmap"
      description="A selected career will become steps: explore, education/pathway, skills, experience, and next actions."
      nextHref="/mentors"
      nextLabel="See mentors"
    />
  );
}
